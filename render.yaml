# render.yaml
services:
  - type: web
    name: 3d-multiplayer-game
    env: node
    buildCommand: npm install
    startCommand: node server/server.js
    envVars:
      - key: NODE_VERSION
        value: 18.x
    healthCheckPath: /
    autoDeploy: true